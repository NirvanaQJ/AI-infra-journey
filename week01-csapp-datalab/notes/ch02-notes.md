# CSAPP 第 2 章精读笔记（一）

日期：2026-09-20
本次范围：信息存储 / 整数表示 / 补码 / 移位运算
待补：2.4 浮点数

## 1. 信息存储
- 字节、字长、虚拟地址空间
- 大端 / 小端：什么时候会真的踩到坑？
- `&` 和 `&&` 的本质区别（位运算 vs 逻辑运算）
- `~` 和 `!` 的区别

## 2. 整数表示
- 无符号编码 B2U、补码编码 B2T 的公式
- 有符号 ↔ 无符号转换：**位模式不变，解释方式变**
- 零扩展 vs 符号扩展
- 截断：为什么 (int)2147483648U 会变成 -2147483648

## 3. 补码
- 为什么负数要设计成"取反加一"？
- TMin 的不对称陷阱：-TMin == TMin，为什么？
- 补码加法的溢出判定

## 4. 移位运算
- 左移补 0；右移分逻辑（补 0）和算术（补符号位）
- 除以 2 的幂：负数要加偏置 `(1<<k)-1` 才能向下取整
- 移位量 >= 字长时行为未定义（C 语言的坑）

## 5. 啊哈时刻（意外的地方）
- 

## 6. 没想通 / 做错的题
- 

## 7. Data Lab 可直接用的技巧
- `~x + 1` = -x
- `x & (x-1)` 清除最低位的 1
- `x & -x` 提取最低位的 1
- `!!x` 归一化任意非 0 为 1

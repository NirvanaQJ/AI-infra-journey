# 2026-09-22（W1 · 周二）

## 今日任务
- [x] CSAPP 2.2-2.3 有符号/无符号转换、扩展与截断（昨晚完成）
- [x] 动手：byte_layout.c + gdb 观察真实字节排布
- [x] 记录：大小端、对齐规则

## 实验产物
- 代码：`week01-csapp-datalab/code/byte_layout.c └─ &i = 取变量 i 的地址
│ │ └──── b = 每单位 1 字节（改成 w 就是 4 字节一单位）
│ └────── x = 十六进制显示
└──────── 16 = 看 16 个单位
实测观察结果（填你的真实输出）核心结论1. 大小端•小端：低位字节放低地址（x86、RISC-V 默认）•大端：高位字节放低地址•人写 0x12345678 从高位写起，小端机器从低位存起，所以 gdb 里看到的是"倒序"2. 和我方向的交点（重点）网络字节序是大端。 这就是为什么跨机通信代码里到处是 htonl() / ntohl() —— 本机是小端，网络包按大端排。以后做 RDMA 抓包、解析 BTH（Base Transport Header） 字段时，字节序搞错，整个包的内容全错。这是今天这个实验和未来工作唯一的、 但极其重要的交点。3. 对齐规则•每个变量必须放在"自身大小整数倍"的地址上（int → 4 的倍数）•原因：硬件按 4/8 字节一次读取，没对齐要读两次再拼接•这也解释了 struct 为什么会有 padding（周五实测）
EOF
quit
